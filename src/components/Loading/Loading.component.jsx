import React from 'react';
import LoadingView from './Loading.styled';

const Loading = () => {
  return (
    <LoadingView>
      <p>Cargando...</p>
    </LoadingView>
  );
};

export default Loading;
